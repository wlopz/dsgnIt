- if user_signed_in?
	%p#intro
		Hello
		= current_user.first_name
		%br/
		%span
			Whats inspiring you? Share it!
- else
	%p#intro
		What's your dsgn?
		%br/
		%span
			Share it!
#posts
	- @posts.each do |post|
		.post
			.post_image
				= link_to (image_tag post.image.url(:small)), post
			.post_content
				.title
					%h2= link_to post.title, post
				.data.clearfix
					%p.username
						Shared by
						= post.user.first_name
					%p.buttons
						%span
							%i.fa.fa-comments-o
							= post.comments.count
						%span
							%i.fa.fa-thumbs-o-up
							= post.get_likes.size


/ - @posts.each do |post|
/  = link_to (image_tag post.image.url(:small)), post
/  %h2= link_to post.title, post
/  %p
/   = post.comments.count
/   Comments
/  %p
/   = post.get_likes.size
/   Likes

/ = link_to "Add New Desgn!", new_post_path